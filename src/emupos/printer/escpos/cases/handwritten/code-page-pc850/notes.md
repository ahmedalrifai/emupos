# code-page-pc850

Hand-written. Every byte from `80` to `ff` on PC850, selected with the Epson
number 2. Bytes the code page leaves undefined, and the few characters no bundled
font has, print the placeholder.
Spec: receipt-printer, "Code page selection".
